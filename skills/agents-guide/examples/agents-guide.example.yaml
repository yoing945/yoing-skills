# 当前目录下 agents-guide 工具的配置文件。
# 复制到目标目录后重命名为 .agents-guide.yaml

tree:
  include:
    - .agents/
    - src/core/
  exclude:
    - temp/

docs:
  include:
    - CHANGELOG.md
    - assets/spec.xlsx
  exclude:
    - DRAFT.md
