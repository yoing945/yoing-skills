[project]
name = "sync-skill"
version = "0.1.0"
description = "Sync skills and prompts to target project"
requires-python = ">=3.9"
dependencies = [
    "pyyaml",
    "pathspec",
]

[project.scripts]
sync-skill = "scripts.main:main"
