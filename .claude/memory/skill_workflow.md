---
name: skill_workflow
description: 本项目技能文件的操作规范与工作流偏好
type: project
---

用户目录下的技能文件是本项目 `skills/` 目录的同步副本。

**规则**：当用户要求增删改查技能时，优先在当前项目仓库的 `skills/` 目录中搜索和操作技能源文件，而非直接操作用户目录下的副本。

**Why:** 项目仓库是技能文件的源头，用户目录只是同步目标。

**How to apply:** 收到技能相关指令时，先用 `Glob` 在项目根目录的 `skills/` 下搜索，找到源文件后再进行读写操作。
